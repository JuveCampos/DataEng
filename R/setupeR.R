# R Setup (En caso de que trabaje en otra compu que no sea la mia)

# Version de R
install.packages("installr")
install.R() # download and run the latest R installer
copy.packages.between.libraries()

# Paquetes basicos
install.packages(c("tidyverse", "stringr", 
                   "rebus", "sf", "leaflet", 
                   "readr", "readxl", "plotly")
